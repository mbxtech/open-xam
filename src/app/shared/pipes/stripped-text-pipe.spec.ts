import { StrippedTextPipe } from './stripped-text-pipe';

describe('StrippedTextPipe', () => {
  it('create an instance', () => {
    const pipe = new StrippedTextPipe();
    expect(pipe).toBeTruthy();
  });
});
